# 推荐：在系统顶部「设置」齿轮 →「大模型 Key」中保存 DeepSeek Key（写入浏览器 localStorage）。
# 开发环境自动走 Vite 代理 /api/deepseek → api.deepseek.com，减轻 CORS 问题。

# 可选：其它 OpenAI 兼容接口（未保存 DeepSeek Key 时生效）
# VITE_LLM_API_KEY=
# VITE_LLM_API_URL=https://api.openai.com/v1/chat/completions
# VITE_LLM_MODEL=gpt-4o-mini

# 可选：开发时 OpenAI 经代理（与 DeepSeek 代理可同时存在）
# VITE_LLM_VIA_PROXY=1
# LLM_API_KEY=
# VITE_LLM_MODEL=gpt-4o-mini

# 企业微信智能表格 Webhook：在「设置 → 智能表格推送」中按业务标识配置多条 URL（写入 localStorage，不经 .env）。
# 新建任务推送使用业务标识 task；其它业务可另增行（如 report_sync），供后续功能调用。
# 若企业微信返回 field not found：在「设置 → 智能表格推送」中配置「任务推送列字段 id」，与当前表列 id 一致。
# 开发环境 npm run dev 时，对 qyapi.weixin.qq.com 的请求会走 Vite 代理 /api/qy-wedoc 以绕过 CORS。
#
# 可选：命令行探针（不走 Vite；用于本机 curl/CI 验证 Webhook）。与 App 默认列 id 一致，列不对时可用 SMARTSHEET_VALUES_JSON 覆盖。
# SMARTSHEET_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/wedoc/smartsheet/webhook?key=…
# npm run smartsheet:test-webhook
